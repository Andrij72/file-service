spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false

  sql:
    init:
      mode: never

  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false

minio:
  enabled: false
  url: http://localhost:9000
  access-key: test
  secret-key: test
  bucket: test-bucket

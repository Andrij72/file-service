spring:
  datasource:
    url: jdbc:mysql://localhost:3310/file_service
    username: usermysql
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8087

minio:
  url: http://localhost:9001
  access-key: minio
  secret-key: minio123
  bucket: microservice-files

eureka:
  client:
    enabled: false
